apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - resources/deployment.yaml
  - resources/service.yaml
configMapGenerator:
  - name: gal-producer-app-configmap-env
    envs:
      - configs/config.env
  - name: gal-producer-app-configmap-file
    files:
      - configs/config.yaml
secretGenerator:
  - name: gal-producer-app-secret-env
    envs:
      - secrets/secret.env
